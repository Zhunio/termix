services:
  termix:
    image: ghcr.io/lukegus/termix:latest
    restart: unless-stopped
    ports:
      - "8080:8080"
    volumes:
      - /opt/termix/data:/app/data
    environment:
      - NODE_ENV=production
